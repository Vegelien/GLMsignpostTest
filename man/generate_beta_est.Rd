% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulationFunctions.R
\name{generate_beta_est}
\alias{generate_beta_est}
\title{#' Generate Estimated Beta Coefficients
#'
#' This function generates an estimate of the beta coefficients using ridge regression.
#' It first generates a design matrix \code{X}, computes the linear predictor \code{eta},
#' and then simulates the response variable \code{Y} based on the given model.
#' Finally, it estimates the beta coefficients using \code{ridgeGLM}.
#'
#' @param true_beta Numeric vector, the true beta coefficients.
#' @param n_beta Integer, the number of samples used for estimation.
#' @param p Integer, the number of predictors.
#' @param model Character, the type of model, e.g., "linear", "logistic", or "poisson".
#' @param lambda_beta Numeric, the regularization parameter for ridge regression.
#' @importFrom porridge ridgeGLM
#' @return A numeric vector of estimated beta coefficients.
generate_beta_est <- function(true_beta, n_beta, p, model, lambda_beta) {
X <- generate_X(n_beta, p)  # Generate design matrix
eta <- calculate_eta(X, 0, true_beta, true_beta)  # Compute linear predictor
Y <- generate_Y(eta, model, n_beta)  # Generate response variable
return(ridgeGLM(Y = Y, X = X, lambda = lambda_beta, model = model))  # Estimate beta coefficients
}
Generate Estimated Beta Coefficients for given X}
\usage{
generate_beta_est(true_beta, n_beta, X, model, lambda_beta)

generate_beta_est(true_beta, n_beta, X, model, lambda_beta)
}
\arguments{
\item{true_beta}{Numeric vector, the true beta coefficients (length p)}

\item{n_beta}{Integer, sample size for estimation}

\item{X}{Numeric matrix, design matrix (n_beta x p)}

\item{model}{Character, one of "linear", "logistic", or "poisson"}

\item{lambda_beta}{Numeric, ridge penalty parameter}
}
\value{
A numeric vector of estimated beta coefficients.

Numeric vector, normalized estimated beta coefficients
}
\description{
This function generates an estimate of the beta coefficients using ridge regression.
It first generates a design matrix \code{X}, computes the linear predictor \code{eta},
and then simulates the response variable \code{Y} based on the given model.
Finally, it estimates the beta coefficients using \code{ridgeGLM}.

Generates an estimate of beta coefficients using ridge regression on simulated data.
The estimated coefficients are normalized to unit length.
}
