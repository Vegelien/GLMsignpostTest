% Generated by hand to match roxygen2 output
\name{run_and_store_power_simulations}
\alias{run_and_store_power_simulations}
\title{Run and store power simulations}
\description{
Execute batches of power simulations using the existing test implementations and
persist the resulting p-values to the power simulation database.
}
\usage{
run_and_store_power_simulations(
  db_path = "power_simulations.db",
  n_grid = c(10, 20, 40, 60, 80, 100, 150),
  p = 100,
  lambda = Inf,
  test_types = c("AS_SW_plugin", "LR"),
  model = "logistic",
  specifications = c("well_specified", "misspecified"),
  n_beta_values = list(NULL, 300),
  lambda_beta = 2,
  B_power = 10,
  B_X = 10,
  gammas = seq(0, 0.5, by = 0.1)
)
}
\arguments{
  \item{db_path}{Path to the SQLite database that stores power simulations.}
  \item{n_grid}{Integer vector of sample sizes.}
  \item{p}{Integer number of predictors.}
  \item{lambda}{Numeric ridge penalty for theta estimation.}
  \item{test_types}{Character vector of test identifiers.}
  \item{model}{Character string selecting the GLM family.}
  \item{specifications}{Character vector describing whether runs are well
  specified or misspecified.}
  \item{n_beta_values}{List containing \code{NULL} (oracle) and/or integers for
  the working coefficient sample sizes.}
  \item{lambda_beta}{Numeric ridge penalty used for coefficient estimation.}
  \item{B_power}{Integer number of Monte Carlo replicates.}
  \item{B_X}{Integer number of design matrices used by the AS test.}
  \item{gammas}{Numeric vector of gamma values.}
}
\value{
Invisibly returns \code{NULL}. The helper is called for its side effects.
}
\examples{
\dontrun{
tmp_db <- tempfile(fileext = ".sqlite")
initialize_power_database(tmp_db)
run_and_store_power_simulations(db_path = tmp_db, B_power = 2, B_X = 2,
                                n_grid = c(10, 20), gammas = c(0, 0.1))
}
}
