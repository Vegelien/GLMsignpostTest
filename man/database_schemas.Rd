% Generated by hand to match roxygen2 output
\name{create_power_database_schema}
\alias{create_power_database_schema}
\alias{create_estimation_database_schema}
\title{Create the simulation database schema}
\description{
Create or update the SQLite schemas used to store simulation parameters and
results.
}
\usage{
create_power_database_schema(con)

create_estimation_database_schema(con)
}
\arguments{
  \item{con}{A \code{\linkS4class{DBIConnection}} obtained from
  \code{DBI::dbConnect()}.}
}
\details{
The power simulation database stores simulation parameters and the resulting
p-values produced by \code{run_and_store_power_simulations()}. The estimation
simulation database stores parameter settings, estimation configurations, and
per-replicate summaries produced by
\code{run_and_store_estimation_simulations()}. Both helpers are safe to call
multiple times for the same database connection.
}
\value{
Invisibly returns \code{NULL}. The helpers are called for their side effects.
}
\examples{
\dontrun{
con <- DBI::dbConnect(RSQLite::SQLite(), tempfile(fileext = ".sqlite"))
create_power_database_schema(con)
create_estimation_database_schema(con)
DBI::dbDisconnect(con)
}
}
