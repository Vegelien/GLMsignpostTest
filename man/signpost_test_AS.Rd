% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signpostTestFunctions.R
\name{signpost_test_AS}
\alias{signpost_test_AS}
\title{Signpost Test for Real Data}
\usage{
signpost_test_AS(
  Y,
  X,
  gammaH0 = 0,
  model = "logistic",
  lambda,
  beta_0,
  beta_a,
  n_beta = NULL,
  lambda_beta = NULL,
  Y_a = NULL,
  X_a = NULL,
  estimate_beta = FALSE,
  use_theta_hat_in_variance = FALSE,
  fisher = FALSE,
  term1ONLY = FALSE,
  lambda_est = lambda,
  scaled = TRUE
)
}
\arguments{
\item{Y}{Numeric vector of response variables (length n)}

\item{X}{Numeric matrix of predictors (n x p)}

\item{gammaH0}{Numeric scalar. Null hypothesis value for gamma parameter (default: 0)}

\item{model}{Character string. Model type, either "logistic" or "poisson" (default: "logistic")}

\item{lambda}{Numeric scalar. Regularization parameter for the test estimation}

\item{beta_0}{Numeric vector. Null hypothesis beta coefficients (length p)}

\item{beta_a}{Numeric vector. Alternative hypothesis beta coefficients (length p)}

\item{n_beta}{Integer. Sample size for beta estimation (required if estimate_beta = TRUE)}

\item{lambda_beta}{Numeric scalar. Regularization parameter for beta estimation (required if estimate_beta = TRUE)}

\item{Y_a}{Numeric vector. Response variables for beta estimation (required if estimate_beta = TRUE)}

\item{X_a}{Numeric matrix. Predictors for beta estimation (required if estimate_beta = TRUE)}

\item{estimate_beta}{Logical. Whether to estimate beta_a from auxiliary data (default: FALSE)}

\item{use_theta_hat_in_variance}{Logical. Whether to use theta_hat in variance calculation (default: FALSE)}

\item{fisher}{Logical. Whether to use Fisher information for standard error (default: FALSE)}

\item{term1ONLY}{Logical. Whether to use only the first term in variance calculation (default: FALSE)}

\item{lambda_est}{Numeric scalar. Regularization parameter for loss estimation (default: lambda)}

\item{scaled}{Logical. Whether to scale estimated beta_a (default: TRUE)}
}
\value{
A list containing:
\describe{
\item{p_value}{Numeric scalar. The computed p-value}
\item{theta_hat}{Numeric scalar. The estimated theta parameter}
\item{std_error}{Numeric scalar. Standard error of theta_hat}
\item{test_statistic}{Numeric scalar. Test statistic (theta_hat / std_error)}
}
}
\description{
Performs a signpost test on real data to test whether the true parameter
lies between two specified alternatives (beta_0 and beta_a).
}
\details{
The signpost test evaluates whether the true parameter lies on the path between
two specified alternatives. The test statistic is based on estimating theta in
the convex combination: beta_true = (1-theta) * beta_0 + theta * beta_a.

When \code{estimate_beta = TRUE}, the function first estimates beta_a using
auxiliary data (Y_a, X_a) with ridge regression.

The standard error can be computed using either Fisher information (\code{fisher = TRUE})
or asymptotic variance based on the sandwich estimator.
}
\examples{
\dontrun{
# Generate example data
set.seed(123)
n <- 100
p <- 10
X <- matrix(rnorm(n * p), nrow = n, ncol = p)
beta_true <- rnorm(p, sd = 0.1)
eta <- X \%*\% beta_true
Y <- rbinom(n, 1, plogis(eta))

# Define hypotheses
beta_0 <- rep(0, p)
beta_a <- rnorm(p, sd = 0.1)
beta_a <- beta_a / sqrt(sum(beta_a^2))

# Perform test
result <- signpost_test_real_data(
  Y = Y, X = X, beta_0 = beta_0, beta_a = beta_a,
  lambda = 0.1, fisher = TRUE
)
print(result)
}

}
\seealso{
\code{\link{ridgeGLM}}, \code{\link{calc_V22}}
}
